# Python AWS CLI

## EC2
```
python-aws-cli ec2 
    --describe-instances
    --monitor <instance id>
    --unmonitor <instance id>
    --start <instance id>
    --stop <instance id>
    --reboot <instance id>
```